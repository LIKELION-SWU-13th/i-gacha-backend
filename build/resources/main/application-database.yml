spring:
  config:
    activate:
      on-profile: local
  datasource:
    username: root
    password: 20030128
    url: "jdbc:mysql://localhost:3306/local?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true"
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10
---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    username: root
    password: 20030128
    url: jdbc:mariadb://svc.sel4.cloudtype.app:31846/igacha
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10
---
